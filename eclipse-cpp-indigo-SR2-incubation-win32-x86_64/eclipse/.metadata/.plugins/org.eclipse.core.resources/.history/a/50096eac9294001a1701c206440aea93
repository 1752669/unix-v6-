#include<stdlib.h>
#include<stddef.h>
#include<sys.h>
#include<stdio.h>
#include<string.h>


int main2()
{
		const char* x="root";
		const char* y="root";
		char* name = NULL;
		char* password = NULL;
		gets(name);
		gets(password);
		if(name == x)
		{	if(password == y)
			{
			printf("mm");
			}
			else
			printf("xx");
		}
		else
		{
			printf("zz");
		}

		for(int i =0;name[i]!='0';i++)
		{	printf(name[i]);
		}
		printf("\n");

		for(int i =0;x[i]!='0';i++)
				{	printf(x[i]);
				}

	return 0;
}
